Name: Cai Chen
NetID: achen2304@iastate.edu

--------------------------------------------------------------

1: findLast

Failing Test: x = [2, 3, 5], y = 2 | Expected: 0 | Actual: -1

(a) What's wrong & the fix: The loop stops too early. It checks the array backward but stops right before index 0. Because of this, it completely misses the first item in the array.
    Fix: Change i > 0 to i >= 0.

(b) Skip the fault entirely: x = [2, 3, 5], y = 5. The number 5 is found instantly at the very end (index 2). The program returns immediately, so it never even reaches the broken condition at index 0.

(c) Run the fault, but get the right answer: x = [2, 3, 5], y = 6. The number 6 isn't in the array at all. The loop skips index 0, but since index 0 isn't 6 anyway, returning -1 is still the correct answer.

(d) Break the internal state, but get the right answer: Not possible. If the program skips the number you are looking for at index 0, it will always return -1 instead of 0. You will always see the failure.

(e) Verification: Changing i > 0 to i >= 0 fixed the code. Running the test x = [2, 3, 5], y = 2 now successfully returns 0.

--------------------------------------------------------------

2: lastZero

Failing Test: x = [0, 1, 0] | Expected: 2 | Actual: 0

(a) What's wrong & the fix: The loop looks forward (from left to right) instead of backward. This means it returns the first zero it sees, not the last one.
    Fix: Change the loop to start at x.length - 1 and decrement (i--) down to 0.

(b) Skip the fault entirely: Not possible. Every time you call this method, the loop automatically runs in the wrong direction. You cannot avoid running the faulty code.

(c) Run the fault, but get the right answer: x = [1, 2, 0]. There is only one zero, and it is at the very end. Whether you search forward or backward, you will find it at index 2, so the final answer is correct.

(d) Break the internal state, but get the right answer: x = [0, 1, 2]. There is only one zero, located at index 0. The broken code finds it instantly and returns 0. A correct backward loop would have checked indices 2 and 1 first before returning 0. The steps taken were wrong (error state), but the final answer is still correct (no failure).

(e) Verification: Reversing the loop fixed the code. Running the test x = [0, 1, 0] now successfully returns 2.

--------------------------------------------------------------

3: countPositive

Failing Test: x = [-4, 2, 0, 2] | Expected: 2 | Actual: 3

(a) What's wrong & the fix: The code treats 0 as a positive number because it uses >= 0. Zero is not positive.
    Fix: Change if (x[i] >= 0) to if (x[i] > 0).

(b) Skip the fault entirely: x = [-4, 2, 3, 2]. There are no zeros in this array. Because there are no zeros to trigger the broken >= logic, the code behaves exactly as it should.

(c) Run the fault, but get the right answer: x = [-3, -1, -4]. Every number is negative. The broken condition evaluates to false every single time, meaning the count stays at 0, which is correct.

(d) Break the internal state, but get the right answer: Not possible. If there is a zero in the array, the broken code will always accidently add 1 to the count. The final total will always be wrong.

(e) Verification: Changing >= 0 to > 0 fixed the code. Running the test x = [-4, 2, 0, 2] now successfully returns 2.

--------------------------------------------------------------

4: oddOrPos

Failing Test: x = [-3, -2, 0, 1, 4] | Expected: 3 | Actual: 2

(a) What's wrong & the fix: The code checks for odd numbers using % 2 == 1. In Java, doing % 2 on a negative odd number (like -3) results in -1, not 1. This means the code completely ignores negative odd numbers.
    Fix: Change x[i] % 2 == 1 to x[i] % 2 != 0.

(b) Skip the fault entirely: x = [2, 4, 6, -2]. There are no negative odd numbers in this array. Without them, the broken math is never exposed, and the code acts perfectly.

(c) Run the fault, but get the right answer: x = [1, 2, 3]. All these numbers are positive, so they automatically pass the second half of the check (x[i] > 0). The broken odd-check runs, but it doesn't matter because the positive check saves it.

(d) Break the internal state, but get the right answer: Not possible. Any time a negative odd number is passed in, the math returns -1, the check fails, and the number is skipped. The final count will always be too low.

(e) Verification: Changing == 1 to != 0 fixed the code. Running the test x = [-3, -2, 0, 1, 4] now successfully returns 3.
